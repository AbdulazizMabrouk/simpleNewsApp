import {StyleSheet} from 'react-native';

export const FONTS = StyleSheet.create({
  regular: {
    fontWeight: 'normal',
  },
  medium: {
    fontWeight: '600',
  },
  bold: {
    fontWeight: 'bold',
  },
});
